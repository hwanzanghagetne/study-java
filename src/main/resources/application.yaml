spring:
  application:
    name: board
  datasource:
    url: jdbc:mysql://localhost:3306/board
    username: root
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
